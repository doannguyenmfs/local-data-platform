# LOCAL DATA PLATFORM — ROADMAP

Trạng thái: ADVANCED IN PROGRESS (core complete; CDC capture complete)
Xác minh end-to-end gần nhất: 2026-09-17

| Level | Chủ đề | Kết quả |
| --- | --- | --- |
| 1 | Airflow fundamentals | DAG, task, dependency, schedule |
| 2 | Advanced orchestration | parallel extraction, retry, validation |
| 3 | Data warehouse | dimensions, SCD2 snapshot, facts, marts |
| 4 | Incremental pipeline | watermark, idempotency, backfill |
| 5 | dbt | staging/intermediate/marts, tests, docs, lineage, CI |
| 6 | Spark | distributed batch transform và Parquet |
| 7 | Iceberg | ACID merge, snapshots, time travel, maintenance |
| 8 | Kafka | replayable events, checkpoint, current state, DLQ |
| 9 | Production integration | Airflow điều phối dbt + Spark + Kafka |
| 10 | Observability | semantic metrics, Prometheus, alerts, Grafana |
| 11 | Delivery | container validation, GitHub Actions, runbook |
| 12 | CDC capture | PostgreSQL WAL, Debezium, slot/publication, c/u/d/tombstone |

Tài liệu học tương ứng nằm trong `docs/learning/01-*.md` đến `12-*.md`.
Topology logical/physical nằm tại `docs/architecture.md`; trách nhiệm từng file
nằm tại `docs/code-map.md`; thao tác vận hành nằm tại `docs/runbook.md`.
Danh sách capability hiện có và advanced backlog chi tiết nằm tại
`docs/platform-capabilities.md`.

## Luồng hoàn chỉnh

```text
extract -> validate -> [dbt, Iceberg batch, Kafka publish] -> advance watermark
                                      Kafka -> Spark stream -> Iceberg
weekly maintenance DAG -> compact files + expire old Iceberg snapshots
all components -> exporter -> Prometheus -> Grafana / Alertmanager
PostgreSQL WAL -> Debezium Connect -> compacted raw CDC topics
```

## Definition of done

- DAG daily và backfill có cửa sổ thời gian rõ ràng.
- Retry extract/dbt/Iceberg/stream không làm nhân đôi business grain.
- Watermark chỉ advance sau mọi downstream bắt buộc thành công.
- dbt kiểm tra schema, grain, relationship, accepted values và SCD2 invariants.
- Spark batch và parser streaming có unit test trong runtime image.
- Iceberg giữ lịch sử snapshot và có maintenance job riêng.
- Kafka topic cố định, producer có deterministic event ID, stream có checkpoint
  và DLQ.
- CI dựng môi trường tối thiểu, chạy test và build toàn bộ image.
- Dashboard/alert theo dõi cả hạ tầng lẫn độ tươi dữ liệu.
- CDC dùng replication role riêng, publication allow-list, durable slot/offset,
  smoke test delete và alert WAL retention.
- README và runbook đủ để khởi động, kiểm tra và phục hồi mà không đọc source.

## Deferred / accepted risks

### Late-arriving source rows

Record xuất hiện muộn nhưng có `updated_at` cũ hơn committed watermark sẽ không
được scheduled incremental run chọn. Tần suất hiện được coi là rất thấp nên xử
lý bằng explicit backfill. CDC hiện capture mọi commit mới của customer không
phụ thuộc `updated_at`; các table batch còn lại vẫn cần ingestion timestamp,
lookback window có dedupe hoặc mở rộng publication sau capacity review.

### CDC materialization/cutover chưa thực hiện

Hard delete customer đã có trong raw Kafka CDC topic nhưng chưa được áp vào
`staging.customers`. Batch extractor vẫn là single writer của staging. Bước
Bronze/Silver sẽ consume envelope, reconcile và cut over source ownership trước
khi retire customer watermark; không chạy hai writer âm thầm trên cùng table.

Vì vậy “Level 12 complete” nghĩa là capture/store/resume/observe đã xong, không
có nghĩa delete đã materialize tới mọi downstream table.

### Local-only constraints

- Single broker/worker/database/object-store: không có HA.
- PLAINTEXT nội bộ, development credentials, không secret manager.
- Alertmanager chưa có external receiver để tránh commit credential.
- Không có off-host backup hoặc disaster-recovery drill.
- Spark gateway là local adapter; production dùng Livy, Spark Operator hoặc
  managed job API.

## Hướng phát triển sau project

Advanced roadmap được ưu tiên và mô tả Definition of Done tại
`docs/platform-capabilities.md`. Thứ tự gần nhất là Schema Registry/contracts,
CDC Bronze/Silver/cutover, rồi external alerting/SLO, backup/DR và security.
