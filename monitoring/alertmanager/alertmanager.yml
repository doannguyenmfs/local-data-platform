# Local receiver intentionally has no external webhook/email credentials. Alerts
# remain visible in the UI; production should add a secret-backed team receiver.
route:
  receiver: local-ui
  group_by: [alertname, severity]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-ui
